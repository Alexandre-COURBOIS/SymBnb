{% extends 'base.html.twig' %}

{% block title %}{{ annonce.title }}{% endblock %}

{% block body %}
    <div class="annonce-title" style="background-image: url({{ annonce.coverImage }})">
        <div class="container">
            <p class="h1">{{ annonce.title }}</p>
            <p>{{ annonce.introduction }}</p>
            <p class="h2">
                <strong>{{ annonce.rooms }} chambres </strong> pour <strong>{{ annonce.price }}&euro;</strong> par nuit
            </p>
            <a href="" class="btn btn-primary">Réserver !</a>
        </div>
    </div>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ annonce.content | raw }}
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for image in annonce.images %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}"
                                {% if loop.first %}class="active"{% endif %}></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for image in annonce.images %}
                            <div class="carousel-item {% if loop.first %}active {% endif %}">
                                <img src="{{ image.url }}" class="d-block w-100" alt="First Slide">
                                <div class="carousel-caption d-none d-md-block">
                                    <h5>{{ annonce.title }}</h5>
                                    <p>{{ image.caption }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img src="http://placehold.it/64x64" alt="avatar de Alexandre">
                    </div>
                    <div class="col">
                        <h3>Alexandre COURBOIS</h3>
                        <span class="badge badge-primary"> 3 Annonces</span>
                    </div>
                </div>
                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Aperiam beatae blanditiis, commodi error
                    excepturi laboriosam officiis quidem unde voluptas voluptate.</p>
                <p>Accusantium assumenda delectus deserunt, esse ex explicabo ipsam magnam non nulla perferendis quo
                    quos recusandae sapiente soluta vero voluptate voluptatum!</p>
                <p>Alias amet consequatur debitis dolorum earum expedita facilis iure laudantium magnam minus
                    perspiciatis placeat quos ratione, sequi ullam vitae voluptatem?</p>
            </div>
        </div>
    </div>
{% endblock %}